rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read for school documents, syllabi, gallery, notices
    // Authenticated admin write
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
